{
  "name": "valjs-mcp-blah",
  "version": "1.0.3",
  "description": "Simple MCP server for Val Town tools",
  "type": "module",
  "private": false,
  "scripts": {
    "build": "esbuild index.ts --outfile=dist/index.cjs --bundle --platform=node --format=cjs",
    "start": "node dist/index.cjs",
    "publish-all": "./publish.sh"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.3.3"
  }
}
